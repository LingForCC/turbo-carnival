// Import Tailwind CSS
import '../core/styles.css';

// Import Snippet Web Component
import './components/snippet-window';

// Initialize the snippet window when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  console.log('Snippet window initialized');
});

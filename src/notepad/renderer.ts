// Import Tailwind CSS
import '../core/styles.css';

// Import Notepad Web Component
import './components/notepad-window';

// Initialize the notepad when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  console.log('Quick Notepad initialized');
});
